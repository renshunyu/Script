#!/bin/sh
export JAVA_HOME=/usr/java/jdk1.7.0_80
export M2_HOME=/usr/java/apache-maven-3.3.9
export PATH=$JAVA_HOME/bin:$M2_HOME/bin:$PATH
export CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar


packagepath=/home/aiuap/svnproject/program30/builddir
svnurl=http://10.1.198.30/svn/UAPPROGRAM/products/ailkiap30/program/reports

cd $packagepath
result=$?
if [ $result != '0' ]
then
  echo "«–ªªƒø¬º¥ÌŒÛ"
  exit 1
fi

if [ -d reports ]; then
  rm -fr reports
fi


svn checkout $svnurl
result=$?
if [ $result != '0' ]
then
  echo "«©≥ˆƒø¬º¥ÌŒÛ"
  exit 1
fi

svn update 

cd reports
result=$?
if [ $result != '0' ]
then
  echo "«–ªªƒø¬º¥ÌŒÛ"
  exit 1
fi


mvn clean package -Dmaven.test.skip=true >./build.log
result=`cat build.log|grep -i "BUILD SUCCESS"|wc -l`
if [ $result != '1' ]
then
  echo "±‡“Î±®¥Ì"
  exit 1
fi
echo "±‡“Î≥…π¶"
